# Контест 7

Ссылка на контест: [Яндекс.Контест](https://contest.yandex.ru/contest/54789/problems/)

## Сводка

| Задача | Статус | Решение |
|--------|--------|---------|
| A | WA | `solutions/A.cpp` |
| B | OK | `solutions/B.cpp` |
| C | OK | `solutions/C.cpp` |
| D | OK | `solutions/D.cpp` |

## Задания

### A. Пирамида (WA)

Реализуйте тип данных «пирамида». Напишите программу, реализовав все указанные здесь методы. Возможные команды для программы:

* «insert n» — Добавить в пирамиду число $n$ $(1 \leq n \leq 10^9)$ (значение $n$ задается после команды). Программа должна вывести `ok`;
* «extract_min» — Удалить из пирамиды минимальный элемент. Программа должна вывести его значение;
* «delete x» Удалить из пирамиды элемент, который был добавлен $x$-м по счёту командой insert $(1 \leq x \leq 200000)$. Программа должна вывести `ok`;
* «change x n» — Изменить значение элемента, который был добавлен $x$-м по счёту командой insert $(1 \leq x \leq 200000)$ на $n$ $(1 \leq n \leq 10^9)$. Программа должна вывести `ok`.
* «get_min» — Программа должна вывести значение минимального элемента, не удаляя его из пирамиды;
* «size» — Программа должна вывести количество элементов в пирамиде;
* «clear» — Программа должна очистить пирамиду и вывести `ok`.

Гарантируется, что в каждый момент времени в пирамиде содержатся только различные элементы.

Если во входных данных встречается операция `extract_min` или `get_min`, и при этом пирамида пуста, то программа должна вместо числового значения вывести строку `error`.

Если во входных данных встречается операция `delete` или `change`, и при этом соответствующего элемента $x$ в пирамиде нет, то программа должна вместо `ok` вывести строку `error`.

#### Формат ввода

В первой строке входных данных записано единственное число $M$ $(1 \leq M \leq 200000)$ — количество команд. В следующих $М$ строках дано по одной команде из тех, что идут выше.

#### Формат вывода

Для каждой команды выведите одну строчку — результат ее выполнения.

#### Пример

- **Ввод**

```text
20
insert 18
extract_min
insert 3
extract_min
insert 10
extract_min
insert 14
insert 20
insert 11
delete 6
extract_min
insert 8
get_min
insert 9
change 7 12
change 4 5
insert 13
change 5 16
insert 19
insert 7
```

- **Вывод**

```text
ok
18
ok
3
ok
10
ok
ok
ok
ok
14
ok
8
ok
ok
error
ok
ok
ok
ok
```

### B. Пирамидальная сортировка

Требуется отсортировать массив с помощью алгоритма пирамидальной сортировки. В конце работы программа должна вывести отсортированный массив.

#### Формат ввода

В первой строке вводится число $N$ — размер массива $(1 \leq N \leq 10^5)$, во второй идут $N$ целых чисел, не превосходящих по модулю $10^9$.

#### Формат вывода

Выведите отсортированный массив, его элементы необходимо разделять пробелами.

#### Пример

- **Ввод**

```text
6
1 2 4 6 5 3
```

- **Вывод**

```text
1 2 3 4 5 6
```

### C. Приоритетная очередь

Требуется реализовать с помощью пирамиды приоритетную очередь, поддерживающую две операции: добавить элемент и извлечь максимальный элемент. При просеивании нельзя совершать лишние перемещения (например, в случае равенства элементов). Если при просеивании вниз, рассматриваемый элемент можно перемещать как влево вниз, так и вправо вниз, то следует выбрать направление влево.

#### Формат ввода

В первой строке вводятся два числа — максимальный размер приоритетной очереди $N$ и количество запросов $M$, $(1 \le M,N \le 10^5)$. Далее идут M строк, в каждой строке — по одному запросу. Первое число в запросе задает его тип, остальные числа (если есть) — параметры запроса. Тип 1 — извлечь максимальный (без параметров). Тип 2 — добавить данный элемент в очередь. Запрос имеет один параметр — число из диапазона $[-10^9;10^9]$

#### Формат вывода

В ответ на запрос типа 1 следует вывести: Если извлекать было нечего (очередь пуста), то -1. Иначе — два числа, первое — индекс конечного положения элемента после его просеивания (если удален последний элемент и просеивать нечего то вывести 0), второе — значение извлеченного элемента.

В ответ на запрос типа 2 следует вывести: Если добавить нельзя (нет места, т.к. в очередь уже N элементов), то -1. Иначе — индекс добавленного элемента.

После выполнения всех запросов требуется вывести пирамиду в её конечном состоянии

#### Пример

- **Ввод**

```text
4 7
1
2 9
2 4
2 9
2 9
2 7
1
```

- **Вывод**

```text
-1
1
2
3
2
-1
2 9
9 4 9
```

### D. Сортировка подсчетом

Вам предлагается отсортировать данные метеостанции, которые к Вам поступили.

Известно, что Вам на вход подается набор целых чисел - округленную температуру.

#### Формат ввода

В первой строке вводится число $N$, $0 \le N \le 10^9$ - количество чисел, которые надо будет отсортировать.

На следующих $N$ строчках следуют числа. Каждое число меньше $10^6$.

#### Формат вывода

Вывести $N$ отсортированных чисел в порядке возрастания, каждое с новой строки.

#### Пример

- **Ввод**

```text
4
34
243
43
292
```

- **Вывод**

```text
34
43
243
292
```

#### Примечания

Интуиция сортировки подсчетом: Пусть мы заранее знаем, что в массиве ограниченное количество разных значений. Может ли это ограничение ускорить сортировку массива? На самом деле да. Давайте попробуем забыть, что мы умеем сравнивать элементы. Что тогда нам известно? Только сколько раз каждый элемент встречается в массиве. Окей, давайте подсчитаем количество вхождений каждого возможного значения, а затем напрямую восстановим отсортированный массив. Это даёт линейную сложность $O(n+k)$, что быстрее $O(n \cdot log(n))$ быстрой сортировки. Единственное ограничение - количество различных значений должно быть не больше количества сортируемых чисел.
